version: '3'
services:
    nginx:
        image: nginx
        restart: always
        ports:
            - 9099:80
        volumes:
            - ./nginx/conf.d:/etc/nginx/conf.d
            - ./frontend/dist:/home/hook1
    api-hooks:
        container_name: api-hooks
        build: ./backend
        ports:
            - 6000:6000